# ──────────────────────────────────────────────────────────────
# Alzheimer's MRI Classifier — FastAPI Backend Dependencies
# ──────────────────────────────────────────────────────────────

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# PyTorch — latest stable (CPU).
# For GPU builds visit: https://pytorch.org/get-started/locally/
torch>=2.6.0
torchvision>=0.21.0

# Image processing
Pillow>=10.0.0
numpy>=1.24.0

# Pydantic (used by FastAPI for request/response validation)
pydantic>=2.0.0

# For file upload support in FastAPI
python-multipart>=0.0.18
